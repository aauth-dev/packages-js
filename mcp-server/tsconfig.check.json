{
  "//": "Local typecheck only. `@aauth/protocol` (WP-1) does not exist in this worktree, so it resolves to the contract-faithful test stub. INTEGRATION: delete this file along with test/protocol-stub/.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "baseUrl": ".",
    "paths": {
      "@aauth/protocol": ["./test/protocol-stub/index.ts"]
    }
  },
  "include": ["src", "test"]
}
